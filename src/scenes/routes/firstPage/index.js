import React from 'react';
import { Grid, Typography } from '@material-ui/core';

export default function FirstPage() {

  return (
    <Grid style={{ backgroundColor: 'green', padding: 30 }}>
      <Typography>Add people</Typography>
    </Grid>
  );
}
