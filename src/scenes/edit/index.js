import React from 'react';
import { Grid, Typography } from '@material-ui/core';

export default function EditPage() {
  return (
    <Grid container style={{ display: "flex", flex: "1 0 auto" }}>
      <Grid item style={{ display: 'flex', flex: '1 0 auto', justifyContent: 'center', alignItems: 'center', paddingTop: "100px" }}>
        <Typography variant='h2'> WIP </Typography>
      </Grid>
    </Grid>
  );
}
