import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
    palette: {
      primary: {
        main: '#f44336',
      },
      secondary: {
        main: '#4caf50',
      },
    },

    typography: {
        fontFamily: 'Roboto'
    },

    overrides: {
    }
});